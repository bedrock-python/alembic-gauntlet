[alembic]
# Paths are set dynamically in tests — these placeholders are never used at runtime.
script_location = tests/integration/sample_app_with_drift/alembic
version_locations = tests/integration/sample_app_with_drift/alembic/versions/clean
path_separator = os
sqlalchemy.url = postgresql+asyncpg://user:pass@localhost/dbname
